import React from "react";

const SearchHistory = () => (
  <div
    style={{
      marginTop: "1rem",
      minHeight: 40,
      background: "#eee",
      borderRadius: 4,
      padding: 8,
    }}
  >
    <em>Search history will be shown here.</em>
  </div>
);

export default SearchHistory;
