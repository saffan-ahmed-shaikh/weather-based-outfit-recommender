import React from "react";

const WeatherDisplay = () => (
  <div
    style={{
      marginBottom: "1rem",
      minHeight: 60,
      background: "#f9f9f9",
      borderRadius: 4,
      padding: 8,
    }}
  >
    <em>Weather info will be shown here.</em>
  </div>
);

export default WeatherDisplay;
